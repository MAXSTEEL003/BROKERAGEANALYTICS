# Copy this file to .env and set your MongoDB connection string

# For local MongoDB (requires MongoDB running locally):
# MONGO_URI=mongodb://localhost:27017/buyersdb

# For MongoDB Atlas (recommended if you don't have local MongoDB):
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/buyersdb?retryWrites=true&w=majority
